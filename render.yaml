databases:
  - name: trial
    databaseName: trial_jcpa
    user: trial_jcpa_user

services:
  - type: web
    name: backenddeploy
    runtime: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: trial
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false